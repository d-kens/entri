spring:
  application:
    name: puuul

  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver

jwt:
  secret: ${JWT_SECRET}
  accessTokenExpiration: 900
  refreshTokenExpiration: 604800

novu:
  secret: ${NOVU_SECRET}