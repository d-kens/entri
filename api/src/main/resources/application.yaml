spring:
  application:
    name: peid

  datasource:
    url: jdbc:mysql://localhost:3306/parrcel_db?createDatabaseIfNotExist=true
    driver-class-name: com.mysql.cj.jdbc.Driver
    username: root
    password: p@RRc3L@@dm!n#2170

  jpa:
    show-sql: true
    hibernate:
      ddl-auto: none

  liquibase:
    change-log: classpath:/db/changelog/changelog.xml
    enabled: true

jwt:
  secret: ${JWT_SECRET}
  accessTokenExpiration: 900 # 15 minutes
  refreshTokenExpiration: 604800 # 7 days

novu:
  secret: ${NOVU_SECRET}

token:
  length: 64
  passwordResetExpirationSeconds: 3600
  emailVerificationExpirationSeconds: 900
  twoFactorAuthExpirationSeconds: 300

app:
  base-url: http://localhost:4200

server:
  port: 8080

mpesa:
  base-url: https://sandbox.safaricom.co.ke
  consumer-key: SECRET_HERE
  consumer-secret: SECRET_HERE