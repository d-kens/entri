spring:
  application:
    name: entri
  servlet:
    multipart:
      max-file-size: 10MB
      max-request-size: 20MB
  liquibase:
    change-log: classpath:db/changelog/changelog.xml
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver

server:
  servlet:
    context-path: /api

jwt:
  secret: ${JWT_SECRET}
  accessTokenExpiration: 900
  refreshTokenExpiration: 604800

app:
  base-url: ${APP_BASE_URL}
  cors:
    allowed-origin: ${APP_CORS_ALLOWED_ORIGIN}
  password-reset:
    expiry-minutes: 15

cookie:
  secure: true

firebase:
  service-account-path: ${FIREBASE_SERVICE_ACCOUNT_PATH:oro-web-app-firebase-adminsdk-fbsvc-8f2ea245fd.json}
  storage:
    bucket: ${FIREBASE_STORAGE_BUCKET:oro-web-app.firebasestorage.app}

novu:
  api:
    key: ${NOVU_SECRET}
  subscriber:
    locale: ${NOVU_SUBSCRIBER_LOCALE:en-KE}
    timezone: ${NOVU_SUBSCRIBER_TIMEZONE:Africa/Nairobi}

management:
  endpoints:
    web:
      exposure:
        include: "health,info"
  info:
    build:
      enabled: true
    env:
      enabled: true