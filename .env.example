# Database
MYSQL_USER=
MYSQL_PASSWORD=
MYSQL_ROOT_PASSWORD=

# API
JWT_SECRET=                           # openssl rand -hex 64
APP_BASE_URL=https://oro.co.ke
APP_CORS_ALLOWED_ORIGIN=https://oro.co.ke
FIREBASE_STORAGE_BUCKET=              # e.g. oro-abc12.firebasestorage.app
NOVU_SECRET=                          # Novu dashboard → Settings → API Keys

# Watchtower — GHCR pull credentials
GHCR_USERNAME=d-kens
GHCR_TOKEN=                           # GitHub → Settings → Developer settings → PAT (classic) → read:packages
